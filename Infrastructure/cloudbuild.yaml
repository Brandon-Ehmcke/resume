steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  args:
  - gcloud
  - functions
  - deploy
  - runWorkflowFunction
  - --region=us-west1
  - --source=.
  - --trigger-http
  - --runtime=nodejs12
